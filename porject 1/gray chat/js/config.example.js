// ─── Supabase Configuration (Copy as config.js and fill in your values) ─────

/**
 * IMPORTANT: Do NOT commit config.js to version control.
 * 
 * 1. Copy this file to `config.js`
 * 2. Replace these placeholder values with your actual Supabase credentials
 * 3. Keep config.js listed in .gitignore
 * 4. Never share these values publicly or commit them to repositories
 */

const SUPABASE_URL = 'https://your-project-id.supabase.co';
const SUPABASE_ANON_KEY = 'your-anon-public-key-here';
